# 랜딩 인트로 롤백 방법

히어로(index.html) 우측 그래픽 자리에서 재생되는 인트로 패널입니다.
**전체화면 오버레이가 아니라 히어로 내부 요소**라서, 재생 중에도
네비게이션·스크롤·팝업이 모두 정상 동작합니다. **JS 없이 CSS 전용**입니다.

마음에 안 들면 아래 2개만 지우면 인트로 도입 전 상태로 완전히 돌아갑니다.

## 1. `index.html` — 인트로 마크업 삭제
`<section class="hero">` 바로 아래의 이 블록을 통째로 삭제:

```
<!-- ══════ 랜딩 인트로 시작 (롤백 시 이 블록 통째로 삭제) ══════ -->
   ... (약 60줄) ...
<!-- ══════ 랜딩 인트로 끝 ══════ -->
```

## 2. `css/style.css` — 스타일 블록 삭제
파일 맨 끝의 이 주석부터 파일 끝까지 삭제:

```
/* ══════════════════════════════════════════════════════════════
   랜딩 인트로 — 히어로 우측 그래픽 자리에 박히는 패널
```

끝입니다. (JS 파일은 없습니다 — 이전 버전의 `js/hero-intro.js` 는 삭제됨)

---

## 동작 방식

- 페이지 로드 때마다 20초 시퀀스가 히어로 우측 패널에서 재생
- **마지막에 패널이 사라지고 원래 히어로 그래픽이 그대로 남습니다**
- `pointer-events: none` 이라 패널 위를 클릭해도 뒤쪽 요소가 반응
- `z-index: 0` — 히어로 카피(1)와 리크루팅 팝업(3000) 아래
- 1100px 이하 / 모션 최소화 설정에서는 표시하지 않음

## 타이밍 수정

CSS 인트로 블록 상단 주석에 구간표가 있습니다.
전체 길이는 **20s**, 씬 표시/숨김은 % 키프레임입니다 (**1초 = 5%**).

### 씬 순서
1. 오프닝 (0.2–2.6s)
2. 프로그램 개요 (2.7–5.0s)
3. Y-Startup 줌아웃 리빌 (5.1–8.3s)
4. Boost Program 줌아웃 리빌 (8.4–11.6s)
5. VC Career Session 줌아웃 리빌 (11.7–14.9s)
6. 파트너 로고 (15.0–17.2s)
7. Y-VENTURES 로고 (17.3–19.2s) → 패널 페이드아웃

### 전체 속도를 바꾸려면
CSS 블록 안의 모든 `20s` 를 원하는 길이로 바꿉니다. 씬 구간은 % 라서
비율이 유지된 채 빨라지거나 느려집니다. 단 **씬 내부 요소는 절대
시각(`animation-delay`)** 이라 함께 맞춰야 합니다:

- 프로그램 줌아웃: `.yvi-p1/p2/p3 .yvi-prog-core` → `5.10s / 8.40s / 11.70s`
- 부제 등장: 각 +0.45s
- 메타 배지(기간·대상): 각 +0.70s 부터 0.08s 간격
- 상세 행: 각 +1.10s 부터 0.18s 간격
- 파트너 마퀴 2줄: `15.20s` / `15.45s`
- 워드마크/태그라인: `17.30s` / `17.90s`

배경 사선(`.yvi-slash`)은 20초 내내 한 방향으로 흐르는 장식이라 별도 시각이
없습니다. (장면 전환 때 지나가던 흰 섬광 `.yvi-swipe` 는 제거됨)

### 프로그램 씬 내용 수정
`index.html` 의 `.yvi-prog` 블록 구조:

```
<div class="yvi-prog-core">
  <b>프로그램명</b>
  <i>한 줄 소개</i>
</div>
<div class="yvi-prog-meta">        ← 기간·대상 배지 (2~3개)
  <span>6주 스프린트</span> ...
</div>
<div class="yvi-prog-items">       ← 단계별 상세 (3개)
  <em><b>제목</b>설명문</em> ...
</div>
```

배지나 행 개수를 바꾸면 CSS 의 `nth-child` delay 도 함께 늘려/줄여주세요.

### 패널 위치·크기
- 폭: `.yvi { width: 62% }`
- 좌측 사선 경계: `clip-path: polygon(39% 0, 100% 0, 100% 100%, 15% 100%)`
  → 히어로 기준 상단 62% / 하단 47% 지점. **사선을 좌우로 옮기려면 이 값만**
  조정하고, 아래 씬 `left` 도 함께 키워/줄여주세요.
- 씬 영역: `.yvi-scene { left: 43%; right: 6% }` — **패널 비율 기반**이라
  화면 폭이 바뀌어도 사선에 잘리거나 히어로 카피와 겹치지 않습니다.
  (클립 상단이 39% 이므로 씬 좌측 43% 는 항상 안전)
- 표시 최소 화면폭: `@media (max-width: 1100px)`

### 파트너 씬
Boost 페이지와 **같은 마퀴 컴포넌트**(`.marquee-row / .marquee / .marquee-track`)를
그대로 씁니다. 로고 목록도 Boost 와 동일합니다 (Partner VCs 9개 ← 방향,
Partner Firms 6개 → 방향, 각각 무한 루프용으로 2배 복제).
`.yvi-marquee-row` 규칙이 패널 크기에 맞게 라벨·간격·아이템(84px)만 축소합니다.
**로고를 추가/변경하면 Boost 페이지와 함께 맞춰주세요.**

### 특정 프로그램만 빼려면
`index.html` 의 해당 `<div class="yvi-scene yvi-prog yvi-pN">` 블록을 지우고,
뒤 씬들의 % 와 delay 를 앞당기면 됩니다.
